// Re-export types for frontend consumption
export type {
  ParsedResume, ExperienceEntry, EducationEntry, ResumeSection,
  ParsedJobDescription, KeywordMatchResult, KeywordMatch, KeywordCategory,
  KeywordCategories, MatchScore, Suggestion, SuggestionCategory,
  AnalysisResult, TailoredResume, ModifiedSection, SessionData, DownloadConfig
} from '../../server/resume-tailor/types'